<% document ||= @document %>

<% if document.has_polygon? %>
  <%= render partial: 'show_geo_checkboxes', locals: { all_id: document.polygon_all_id, all_i18n_key: 'geoblacklight.show.geospatial_info.select.polygon', checkbox_class: 'polygon', prefix: document.polygon_prefix, values: document.polygons } %>
<% end %>
