<% document ||= @document %>

<% if document.has_bbox? %>
  <%= render partial: 'show_geo_checkboxes', locals: { all_id: document.bbox_all_id, all_i18n_key: 'geoblacklight.show.geospatial_info.select.bbox', checkbox_class: 'bbox', prefix: document.bbox_prefix, values: document.bboxes } %>
<% end %>
