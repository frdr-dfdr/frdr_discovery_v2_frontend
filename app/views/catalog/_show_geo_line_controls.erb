<% document ||= @document %>

<% if document.has_line? %>
  <%= render partial: 'show_geo_checkboxes', locals: { all_id: 'line-all', all_i18n_key: 'geoblacklight.show.geospatial_info.select.line', checkbox_class: 'line', prefix: 'line-', values: document.lines } %>
<% end %>
